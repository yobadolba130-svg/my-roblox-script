if not game:IsLoaded() then game.Loaded:Wait() endlocal S = Instance.new("ScreenGui")local M = Instance.new("Frame")local T = Instance.new("Frame")local L = Instance.new("TextLabel")local C = Instance.new("TextButton")local SF = Instance.new("ScrollingFrame")local LL = Instance.new("UIListLayout")S.Name = "GrandpaMegaMenu"S.Parent = game:GetService("CoreGui") or game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")S.ResetOnSpawn = falseM.Name = "MainFrame"M.Parent = SM.BackgroundColor3 = Color3.fromRGB(25, 25, 25)M.Position = UDim2.new(0.05, 0, 0.1, 0)M.Size = UDim2.new(0, 260, 0, 430)M.BorderSizePixel = 0M.Active = trueM.Draggable = trueT.Name = "TopBar"T.Parent = MT.BackgroundColor3 = Color3.fromRGB(40, 40, 40)T.Size = UDim2.new(1, 0, 0, 35)T.BorderSizePixel = 0L.Name = "Title"L.Parent = TL.BackgroundTransparency = 1L.Position = UDim2.new(0, 12, 0, 0)L.Size = UDim2.new(1, -50, 1, 0)L.Font = Enum.Font.SourceSansBoldL.Text = "Rostov God Menu v8"L.TextColor3 = Color3.fromRGB(255, 255, 255)L.TextSize = 16L.TextXAlignment = Enum.TextXAlignment.LeftC.Name = "CloseButton"C.Parent = TC.BackgroundColor3 = Color3.fromRGB(160, 45, 45)C.Position = UDim2.new(1, -30, 0, 5)C.Size = UDim2.new(0, 25, 0, 25)C.Font = Enum.Font.SourceSansBoldC.Text = "X"C.TextColor3 = Color3.fromRGB(255, 255, 255)C.TextSize = 14C.BorderSizePixel = 0SF.Name = "ScrollingFrame"SF.Parent = MSF.BackgroundTransparency = 1SF.Position = UDim2.new(0, 0, 0, 40)SF.Size = UDim2.new(1, 0, 1, -45)SF.CanvasSize = UDim2.new(0, 0, 0, 560)SF.ScrollBarThickness = 6LL.Parent = SFLL.SortOrder = Enum.SortOrder.LayoutOrderLL.Padding = UDim.new(0, 8)LL.HorizontalAlignment = Enum.HorizontalAlignment.Centerlocal p = game:GetService("Players").LocalPlayerlocal uis = game:GetService("UserInputService")local rs = game:GetService("RunService")local ps = game:GetService("Players")local ij, nc, fly, swf = false, false, false, falselocal esp, skel, ClickTp, oxy = false, false, false, falselocal speed, jump, act = 16, 50, truelocal aCol = Color3.fromRGB(0, 255, 0)local eCol = Color3.fromRGB(255, 0, 0)local fCon, bV, bGlocal function stopFly()fly = falseif fCon then fCon:Disconnect() fCon = nil endif bV then bV:Destroy() bV = nil endif bG then bG:Destroy() bG = nil endpcall(function()if p.Character and p.Character:FindFirstChildOfClass("Humanoid") thenp.Character:FindFirstChildOfClass("Humanoid").PlatformStand = falseendend)endlocal function startFly()stopFly()if swf then swf = false endfly = truelocal char = p.Characterlocal hum = char and char:FindFirstChildOfClass("Humanoid")local root = char and char:FindFirstChild("HumanoidRootPart")if not root or not hum then return endhum.PlatformStand = truebG = Instance.new("BodyGyro")bG.P = 9e4bG.maxTorque = Vector3.new(9e9, 9e9, 9e9)bG.cframe = root.CFramebG.Parent = rootbV = Instance.new("BodyVelocity")bV.velocity = Vector3.new(0, 0.1, 0)bV.maxForce = Vector3.new(9e9, 9e9, 9e9)bV.Parent = rootlocal cam = workspace.CurrentCamerafCon = rs.RenderStepped:Connect(function()if not fly or not root or not p.Character then stopFly() return endlocal dir = Vector3.new(0, 0, 0)if uis:IsKeyDown(Enum.KeyCode.W) then dir = dir + cam.CFrame.LookVector endif uis:IsKeyDown(Enum.KeyCode.S) then dir = dir - cam.CFrame.LookVector endif uis:IsKeyDown(Enum.KeyCode.A) then dir = dir - cam.CFrame.RightVector endif uis:IsKeyDown(Enum.KeyCode.D) then dir = dir + cam.CFrame.RightVector endif uis:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) endif uis:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0, 1, 0) endbG.cframe = cam.CFrameif dir.Magnitude > 0 thenbV.velocity = dir.Unit * speedelsebV.velocity = Vector3.new(0, 0, 0)endend)endlocal function clearEsp()for _, pl in ipairs(ps:GetPlayers()) doif pl.Character thenlocal f = pl.Character:FindFirstChild("GrandpaESP")if f then f:Destroy() endendendendlocal function parseColor(t, d)local r, g, b = t:match("(%d+)%s+(%d+)%s+(%d+)")if r and g and b thenreturn Color3.fromRGB(tonumber(r), tonumber(g), tonumber(b))endreturn dendC.MouseButton1Click:Connect(function()act = falseij = falsenc = falseswf = falseesp = falseskel = falseClickTp = falseoxy = falsestopFly()clearEsp()pcall(function()local char = p.Characterif char thenlocal hum = char:FindFirstChildOfClass("Humanoid")if hum thenhum.WalkSpeed = 16hum.PlatformStand = falseif hum.UseJumpPower thenhum.JumpPower = 50elsehum.JumpHeight = 50 / 3.5endhum:ChangeState(Enum.HumanoidStateType.GettingUp)endfor _, part in ipairs(char:GetDescendants()) doif part:IsA("BasePart") then part.CanCollide = true endendendend)S:Destroy()end)local function createToggle(name, cb)local btn = Instance.new("TextButton")btn.Parent = SFbtn.Size = UDim2.new(0, 230, 0, 32)btn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)btn.Font = Enum.Font.SourceSansbtn.Text = name .. ": OFF"btn.TextColor3 = Color3.fromRGB(240, 100, 100)btn.TextSize = 14btn.BorderSizePixel = 0local en = falsebtn.MouseButton1Click:Connect(function()en = not enif en thenbtn.BackgroundColor3 = Color3.fromRGB(70, 110, 70)btn.TextColor3 = Color3.fromRGB(100, 240, 100)btn.Text = name .. ": ON"elsebtn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)btn.TextColor3 = Color3.fromRGB(240, 100, 100)btn.Text = name .. ": OFF"endcb(en)end)endlocal function createInput(ph, dv, cb)local con = Instance.new("Frame")con.Parent = SFcon.Size = UDim2.new(0, 230, 0, 32)con.BackgroundTransparency = 1local tb = Instance.new("TextBox")tb.Parent = contb.Size = UDim2.new(0, 150, 1, 0)tb.BackgroundColor3 = Color3.fromRGB(55, 55, 55)tb.Font = Enum.Font.SourceSanstb.Text = tostring(dv)tb.PlaceholderText = phtb.TextColor3 = Color3.fromRGB(255, 255, 255)tb.TextSize = 14tb.BorderSizePixel = 0local ok = Instance.new("TextButton")ok.Parent = conok.Position = UDim2.new(0, 160, 0, 0)ok.Size = UDim2.new(0, 70, 1, 0)ok.BackgroundColor3 = Color3.fromRGB(40, 100, 160)ok.Font = Enum.Font.SourceSansBoldok.Text = "OKAY"ok.TextColor3 = Color3.fromRGB(255, 255, 255)ok.TextSize = 12ok.BorderSizePixel = 0ok.MouseButton1Click:Connect(function() cb(tb.Text) end)endlocal tc = Instance.new("Frame")tc.Parent = SFtc.Size = UDim2.new(0, 230, 0, 32)tc.BackgroundTransparency = 1local ttb = Instance.new("TextBox")ttb.Parent = tcttb.Size = UDim2.new(0, 150, 1, 0)ttb.BackgroundColor3 = Color3.fromRGB(55, 55, 55)ttb.Font = Enum.Font.SourceSansttb.Text = ""ttb.PlaceholderText = "Player Nickname"ttb.TextColor3 = Color3.fromRGB(255, 255, 255)ttb.TextSize = 14ttb.BorderSizePixel = 0local tbtn = Instance.new("TextButton")tbtn.Parent = tctbtn.Position = UDim2.new(0, 160, 0, 0)tbtn.Size = UDim2.new(0, 70, 1, 0)tbtn.BackgroundColor3 = Color3.fromRGB(120, 60, 160)tbtn.Font = Enum.Font.SourceSansBoldtbtn.Text = "TP"tbtn.TextColor3 = Color3.fromRGB(255, 255, 255)tbtn.TextSize = 12tbtn.BorderSizePixel = 0tbtn.MouseButton1Click:Connect(function()local tName = ttb.Text:lower()if tName ~= "" thenfor _, pl in ipairs(ps:GetPlayers()) doif pl ~= p and (pl.Name:lower():find(tName) or pl.DisplayName:lower():find(tName)) thenif pl.Character and pl.Character:FindFirstChild("HumanoidRootPart") and p.Character and p.Character:FindFirstChild("HumanoidRootPart") thenp.Character.HumanoidRootPart.CFrame = pl.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 2)breakendendendendend)createInput("Speed", speed, function(v) local n = tonumber(v) if n then speed = n end end)createInput("Jump", jump, function(v) local n = tonumber(v) if n then jump = n end end)createInput("Ally RGB (0 255 0)", "0 255 0", function(v) aCol = parseColor(v, aCol) clearEsp() end)createInput("Enemy RGB (255 0 0)", "255 0 0", function(v) eCol = parseColor(v, eCol) clearEsp() end)createToggle("Inf Jump", function(s) ij = s end)createToggle("Noclip", function(s) nc = s end)createToggle("Normal Fly", function(s) if s then startFly() else stopFly() end end)createToggle("Swim Fly", function(s) swf = s if s then stopFly() end end)createToggle("Advanced 3D ESP", function(s) esp = s if not s then clearEsp() end end)createToggle("Show Skeletons", function(s) skel = s if not s then clearEsp() end end)createToggle("Click TP (Ctrl+LClick)", function(s) ClickTp = s end)createToggle("Infinite Oxygen", function(s) oxy = s end)uis.InputBegan:Connect(function(i, gpe)if not gpe and i.KeyCode == Enum.KeyCode.K and M then M.Visible = not M.Visible endif not gpe and ClickTp and i.UserInputType == Enum.UserInputType.MouseButton1 and uis:IsKeyDown(Enum.KeyCode.LeftControl) thenlocal ms = p:GetMouse()if ms and p.Character and p.Character:FindFirstChild("HumanoidRootPart") thenp.Character.HumanoidRootPart.CFrame = CFrame.new(ms.Hit.p) + Vector3.new(0, 3, 0)endendend)uis.JumpRequest:Connect(function()if ij and not fly thenpcall(function()local h = p.Character and p.Character:FindFirstChildOfClass("Humanoid")if h then h:ChangeState(Enum.HumanoidStateType.Jumping) endend)endend)local function drawBone(pChar, folder, partA, partB, color)local a, b = pChar:FindFirstChild(partA), pChar:FindFirstChild(partB)if a and b thenlocal line = folder:FindFirstChild(partA .. "" .. partB) or Instance.new("LineHandleAdornment")line.Name = partA .. "" .. partBline.Length = (a.Position - b.Position).Magnitudeline.CFrame = CFrame.lookAt(a.Position, b.Position)line.Color3 = colorline.AlwaysOnTop = trueline.Thickness = 2line.ZIndex = 4line.Adornee = aline.Parent = folderendendtask.spawn(function()while act and task.wait(0.05) dopcall(function()local char = p.Characterif char thenlocal hum = char:FindFirstChildOfClass("Humanoid")if hum thenif swf then hum:ChangeState(Enum.HumanoidStateType.Swimming) endif not fly thenif hum.WalkSpeed ~= speed then hum.WalkSpeed = speed endif hum.UseJumpPower thenif hum.JumpPower ~= jump then hum.JumpPower = jump endelselocal conv = jump / 3.5if hum.JumpHeight ~= conv then hum.JumpHeight = conv endendendif oxy and hum:GetAttribute("Oxygen") then hum:SetAttribute("Oxygen", 100) endendif nc thenfor _, part in ipairs(char:GetDescendants()) doif part:IsA("BasePart") and part.CanCollide then part.CanCollide = false endendendendif esp or skel thenfor _, pl in ipairs(ps:GetPlayers()) doif pl ~= p and pl.Character and pl.Character:FindFirstChild("HumanoidRootPart") and pl.Character:FindFirstChildOfClass("Humanoid") thenlocal pChar = pl.Characterlocal pHum = pChar:FindFirstChildOfClass("Humanoid")local root = pChar.HumanoidRootPartlocal isAlly = (pl.Team == p.Team and p.Team ~= nil)local color = isAlly and aCol or eCollocal folder = pChar:FindFirstChild("GrandpaESP") or Instance.new("Folder", pChar)folder.Name = "GrandpaESP"if esp thenlocal box = folder:FindFirstChild("3DBox") or Instance.new("BoxHandleAdornment", folder)box.Name = "3DBox"box.Size = pChar:GetExtentsSize() + Vector3.new(0.2, 0.2, 0.2)box.AlwaysOnTop = truebox.ZIndex = 3box.Color3 = colorbox.Adornee = pCharbox.Transparency = 0.6local dist = math.floor((p.Character and p.Character:FindFirstChild("HumanoidRootPart") and (p.Character.HumanoidRootPart.Position - root.Position).Magnitude) or 0)local tag = folder:FindFirstChild("ESPTag") or Instance.new("BillboardGui", folder)tag.Name = "ESPTag"tag.Size = UDim2.new(0, 200, 0, 50)tag.AlwaysOnTop = truetag.ExtentsOffset = Vector3.new(0, 3, 0)tag.Adornee = rootlocal txt = tag:FindFirstChild("ESPText") or Instance.new("TextLabel", tag)txt.Name = "ESPText"txt.BackgroundTransparency = 1txt.Size = UDim2.new(1, 0, 1, 0)txt.Font = Enum.Font.SourceSansBoldtxt.TextSize = 14txt.TextColor3 = colortxt.TextStrokeTransparency = 0txt.Text = string.format("%s\nHP: %d | Dist: %d", pl.Name, math.floor(pHum.Health), dist)elselocal b = folder:FindFirstChild("3DBox") if b then b:Destroy() endlocal t = folder:FindFirstChild("ESPTag") if t then t:Destroy() endendif skel thenif pChar:FindFirstChild("Left Upper Leg") thendrawBone(pChar, folder, "Head", "UpperTorso", color)drawBone(pChar, folder, "UpperTorso", "LowerTorso", color)drawBone(pChar, folder, "UpperTorso", "LeftUpperArm", color)drawBone(pChar, folder, "LeftUpperArm", "LeftLowerArm", color)drawBone(pChar, folder, "UpperTorso", "RightUpperArm", color)drawBone(pChar, folder, "RightUpperArm", "RightLowerArm", color)drawBone(pChar, folder, "LowerTorso", "LeftUpperLeg", color)drawBone(pChar, folder, "LeftUpperLeg", "LeftLowerLeg", color)drawBone(pChar, folder, "LowerTorso", "RightUpperLeg", color)drawBone(pChar, folder, "RightUpperLeg", "RightLowerLeg", color)elsedrawBone(pChar, folder, "Head", "Torso", color)drawBone(pChar, folder, "Torso", "Left Arm", color)drawBone(pChar, folder, "Torso", "Right Arm", color)drawBone(pChar, folder, "Torso", "Left Leg", color)drawBone(pChar, folder, "Torso", "Right Leg", color)endelsefor _, item in ipairs(folder:GetChildren()) doif item:IsA("LineHandleAdornment") then item:Destroy() endendendendendendend)endend)
